import {atom} from 'recoil';

const eCount = atom({
  key: 'eCount',
  default: 0,
});

const iCount = atom({
  key: 'iCount',
  default: 0,
});

const sCount = atom({
  key: 'sCount',
  default: 0,
});

const wCount = atom({
  key: 'wCount',
  default: 0,
});

const uCount = atom({
  key: 'uCount',
  default: 0,
});

const fCount = atom({
  key: 'fCount',
  default: 0,
});

const rCount = atom({
  key: 'rCount',
  default: 0,
});

const vCount = atom({
  key: 'vCount',
  default: 0,
});

const aCount = atom({
  key: 'aCount',
  default: 0,
});

const pCount = atom({
  key: 'pCount',
  default: 0,
});

export {
  eCount,
  iCount,
  sCount,
  wCount,
  uCount,
  fCount,
  rCount,
  vCount,
  aCount,
  pCount,
};
